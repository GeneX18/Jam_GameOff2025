using UnityEngine;
using UnityEngine.Rendering.Universal;

public class StepFadeOut : MonoBehaviour
{
    /*
    float timer = 0.0f;
    float fadeValue = 1.0f;
    */

    

    void Update()
    {
        /*
        if (timer < 3.0f)
        {
            timer += Time.deltaTime;
            return;
        }

        fadeValue -= Time.deltaTime * 1.5f;
        transform.GetComponent<DecalProjector>().material.SetFloat("_Opacity", fadeValue);

        if (fadeValue < -1.0f)
        {
            DestroyImmediate(transform.gameObject);
        }
        */
    }

    
}
